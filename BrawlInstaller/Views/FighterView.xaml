<UserControl x:Class="BrawlInstaller.Views.FighterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrawlInstaller.Views"
             xmlns:viewModels="clr-namespace:BrawlInstaller.ViewModels" d:DataContext="{d:DesignInstance Type=viewModels:FighterViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox x:Name="costumeListBox" ItemsSource="{Binding Costumes}" DisplayMemberPath="CostumeId" Margin="204,120,372,200" SelectedValue="{Binding SelectedCostume, Mode=TwoWay}"/>
        <Button x:Name="loadButton" Content="Button" HorizontalAlignment="Left" Margin="296,346,0,0" VerticalAlignment="Top" Command="{Binding LoadCommand}"/>
        <Image x:Name="cspImage" HorizontalAlignment="Left" Height="100" Margin="571,250,0,0" VerticalAlignment="Top" Width="100" Source="{Binding SelectedCostume.CosmeticViewModels[0].Cosmetics[0].Image}"/>
        <Image x:Name="bpImage" HorizontalAlignment="Left" Height="100" Margin="487,131,0,0" VerticalAlignment="Top" Width="100" Source="{Binding SelectedCostume.CosmeticViewModels[1].Cosmetics[0].Image}"/>

    </Grid>
</UserControl>
