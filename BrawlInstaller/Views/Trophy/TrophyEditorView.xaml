<UserControl x:Class="BrawlInstaller.Views.TrophyEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrawlInstaller.Views"
             xmlns:userControls="clr-namespace:BrawlInstaller.UserControls"
             xmlns:resources="clr-namespace:BrawlInstaller.Resources" d:DataContext="{d:DesignInstance Type=viewModels:TrophyEditorViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label x:Name="nameLabel" Content="Name:"/>
        <TextBox x:Name="nameTextBox" Text="{Binding Trophy.Name}" Grid.Column="1"/>
    </Grid>
</UserControl>
