<UserControl x:Class="BrawlInstaller.Views.TracklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrawlInstaller.Views"
             xmlns:resources="clr-namespace:BrawlInstaller.Resources"
             xmlns:userControls="clr-namespace:BrawlInstaller.UserControls"
             xmlns:viewModels="clr-namespace:BrawlInstaller.ViewModels" d:DataContext="{d:DesignInstance Type=viewModels:TracklistViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="1"/>
        <Grid>
            <GroupBox x:Name="tracklistGroupBox" Header="Tracklists" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Label x:Name="tracklistLabel" Content="Tracklists" HorizontalAlignment="Left"/>
                            <ListBox x:Name="tracklistsListBox" ItemsSource="{Binding Tracklists}" SelectedValue="{Binding SelectedTracklist}" Width="120" Height="400" Grid.Row="1">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Path=., Converter={StaticResource filePathNoExtensionConverter}}" ToolTip="{Binding}"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </ScrollViewer>
                    <StackPanel Grid.Row="1">
                        <Button x:Name="loadButton" Content="Load" Command="{Binding LoadTracklistCommand}"/>
                        <Button x:Name="newButton" Content="New"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
        <Grid Grid.Column="1" Margin="3,0,0,0" Visibility="{Binding LoadedTracklist, Converter={StaticResource nullVisConverter}}">
            <GroupBox Header="Editor" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Label x:Name="tracklistNameLabel" Content="Name:"/>
                                <TextBox x:Name="tracklistNameTextBox" Text="{Binding LoadedTracklist.Name}" Width="120"/>
                            </StackPanel>
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Label x:Name="songListLabel" Content="Songs:" HorizontalAlignment="Left"/>
                                <StackPanel Grid.Row="1">
                                    <ListBox x:Name="songListBox" ItemsSource="{Binding TracklistSongs}" SelectedItem="{Binding SelectedSong}" DisplayMemberPath="Name" Width="140" Height="200"/>
                                </StackPanel>
                                <Grid x:Name="songPropertiesGrid" Grid.Row="1" Grid.Column="1" Visibility="{Binding SelectedSong, Converter={StaticResource nullVisConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Label x:Name="songNameLabel" Content="Name:"/>
                                    <TextBox x:Name="songNameTextBox" Text="{Binding SelectedSong.Name}" Width="100" Grid.Column="1"/>
                                    <Label x:Name="songPathLabel" Grid.Row="1" Content="Path:"/>
                                    <TextBox x:Name="songPathTextBox" Grid.Row="1" Text="{Binding SelectedSong.SongPath}" Width="100" Grid.Column="1"/>
                                    <Label x:Name="songFileLabel" Grid.Row="2" Content="File:"/>
                                    <userControls:FileBox x:Name="songFileBox" Grid.Row="2" Text="{Binding SelectedSong.SongFile}" TextBoxWidth="100" Grid.Column="1" Filter="BRSTM file (.brstm)|*.brstm"/>
                                    <Label x:Name="songIdLabel" Grid.Row="3" Content="ID:"/>
                                    <TextBox x:Name="songIdTextBox" Grid.Row="3" Text="{Binding SelectedSong.SongId, Converter={StaticResource hexUIntConverter}}" Width="100" Grid.Column="1"/>
                                    <Label x:Name="songDelayLabel" Grid.Row="4" Content="Delay:"/>
                                    <TextBox x:Name="songDelayTextBox" Grid.Row="4" Text="{Binding SelectedSong.SongDelay}" Width="100" Grid.Column="1"/>
                                    <Label x:Name="volumeLabel" Grid.Row="5" Content="Volume:"/>
                                    <TextBox x:Name="volumeTextBox" Grid.Row="5" Text="{Binding SelectedSong.Volume}" Width="100" Grid.Column="1"/>
                                    <Label x:Name="frequencyLabel" Grid.Row="6" Content="Frequency:"/>
                                    <TextBox x:Name="frequencyTextBox" Grid.Row="6" Text="{Binding SelectedSong.Frequency}" Width="100" Grid.Column="1"/>
                                    <Label x:Name="songSwitchLabel" Grid.Row="7" Content="Song Switch:"/>
                                    <TextBox x:Name="songSwitchTextBox" Grid.Row="7" Text="{Binding SelectedSong.SongSwitch}" Width="100" Grid.Column="1"/>
                                    <CheckBox x:Name="stockPinchCheckBox" Content="Disable Stock Pinch" IsChecked="{Binding SelectedSong.DisableStockPinch}" Grid.Column="2"/>
                                    <CheckBox x:Name="hiddenFromTracklistCheckBox" Content="Hidden from Tracklist" IsChecked="{Binding SelectedSong.HiddenFromTracklist}" Grid.Row="1" Grid.Column="2"/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </ScrollViewer>
                    <WrapPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
                        <Button Content="Test"/>
                    </WrapPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>
